{
  "name": "food-safety-provenance",
  "version": "0.1.0",
  "description": "Blockchain-Based Supply Chain Provenance System for Food Safety Tracking — CSE 540",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  }
}
